using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CoinConstants : MonoBehaviour
{

    // There's probably a better way to easily set up the coins to reference various other objects,
    // without having to set them individually, but this was the way I thought to do it.
    public GameObject player;
    public GameObject scorePanel;

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
